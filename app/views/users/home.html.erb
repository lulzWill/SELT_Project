<h1>Welcome <%=@current_user.user_id%></h1>

<% if @current_user.role != "Admin" %>
<div id='calendar'></div>


<h2>Courses</h2>
<% puts @current_user.courses %>
<% for course in @current_user.courses%>
<%= link_to "#{course.name}", course_path(course)%>
<br>
<% end %>
<% else %>
<%= link_to "View Courses", courses_path %>
<% end %>
<br>
<% if @current_user.role != "Student" %>
<%= link_to "View Students", view_students_path%>
<br>
<% end %>
<% if @current_user.role == "Admin" %>
<%= link_to "View Teachers", '/users/admin_view_professors' %>
<br>
<% else %>
<%= link_to "Enroll in a course", courses_path%>
<% end %>